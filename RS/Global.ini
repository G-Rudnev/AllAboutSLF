Appliable fields is the only those ones that end with double semicolon and got an equal-colon sign in it. 
The newline symbol after 2x-semicolon does not matter for in-row comments possibility.
Do not write comments inside entries (after rows delimeters)!
It is legit if place such a fields everywhere, but the key of a field considered as a string of all the symbols preceding an colon-equal sign after 
the previous 2x semicolon or after the begining of a line.
If entries have the same names the previous ones ignored.
Spaces assume as delimiters of columns, commas + newline - of rows.

!!! THIS PARAMETERS ARE OPERATOR EXCHANGE PARAMETERS

group:=params;;

MESSAGE:=0;;

MAINDEVICES:=0;;    # 0 - Ok; 1 - tag not ready; 2,4,8,16 - lidars not ready

____MAIN FLAGS____
ACTION:=0;;
LOCATED:=0;;
REVEAL:=0;;
CLEAR:=0;;   # clear the route, if moving smoothly stops the current main route and clear it but has no effect on alternate route

FLEA:=1;;

____MOTION PARAMETERS____ 
speedGain:=100;;    # <= 1.0
safetyBox:=12;;   # % of robo bounding box, in addition to it

______ROBO______
vs0Abs:=0.5             # basic straight forward unsigned speed
vsMinAbs:=0.02          # minimum forward or wheel speed
valphaMaxAbs:=1.45    # rad/s - maximum rotation speed
speedStepAbs:=0.06      # step of speed changing
speedStepDelay:=0.02    # delay of speed stepping, speedStepAbs/speedStepDelay ratio in fact equals acceleration
tMin:=0.3               # minimum route iteration time

______FLEA______
vs0Abs:=0.52;;            # basic straight forward unsigned speed (0.5765)
vsMinAbs:=0.005;;          # minimum forward or wheel speed
valphaMaxAbs:=2.0943951;;    # rad/s - maximum rotation speed (2.0943951)
speedStepAbs:=0.15;;      # step of speed changing
speedStepDelay:=0.01;;    # delay of speed stepping, speedStepAbs/speedStepDelay ratio in fact equals acceleration
tMin:=0.3;;               # minimum route iteration time

____CHASSIS DIMENSIONS____
______ROBO______
r0:=0.4
alpha0:=0.7854        # pi / 4.0
half_length:=0.57     # half a length
half_width:=0.3     # half a width
half_base:=0.0     # half a longitudinal distance between wheels
half_track:=0.265     # half a lateral distance between wheels
wheel_diameter:=0.15

______FLEA______
r0:=0.1;;
alpha0:=0.5236;;        # pi / 6.0
half_length:=0.128;;     # half a length
half_width:=0.08;;     # half a width
half_base:=0.054;;     # half a longitudinal distance between wheels
half_track:=0.113;;     # half a lateral distance between wheels
wheel_diameter:=0.065;;

____ROUTE AND POSITION DATA____
arcs:=0;;
arcsL2G0:=0;;
iterator:=0;;
rL2G:=0;;
eL2G:=0;;

____FILTERS____
egain:=50;;     # non-inertial gain (0..20 %)
mgain:=100;;      # inertial gain (%)
cor_hpf:=50;;   #(%)
rtls_base:=30;; #cm

____PERIPHERALS____
us_thres:=150;; #mm
voltage_thres:=24.0;;
input_voltage:=0.0;;
fresh_water:=0;;
dirty_water:=0;;

____ANCHORS____
Anchor0:=0;;
Anchor1:=0;;
Anchor2:=0;;
Anchor3:=0;;
Anchor4:=0;;    # are to be captured
noAnchorsLimit:=100;;
anchorsEnough:=4;; #for locating

____CLEANER____
cleaner:=0;;
water:=50;;

____JOYSTICK____
joyS:=0.0;;
joyAlpha:=0.0;;
